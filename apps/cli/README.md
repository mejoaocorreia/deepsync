# deepsync CLI

Commands: `status`, `doctor`, `plan add`, `apply`, `rollback`, and `recover`. Every command supports structured JSON output. Apply performs recovery before mutation, and terminal failure outcomes use stable nonzero exit codes. See the repository quickstart for the supported isolated DSH workflow.
