# @deepsync/doctor

Deterministic checks run before any optional future AI analysis. Every check returns timestamped pass, fail, warning, or skipped evidence; thrown checks become contained failures without preventing independent checks.
